{% extends 'admin/_layout.html.twig' %}

{% block admin_title %}
Dashboard Administrateur
{% endblock %}

{% block admin_content %}

{# ================================
    STATS PRINCIPALES
================================ #}
<div class="row g-4 mb-5">

     <div class="col-md-4">
          <div class="card border-0 shadow-sm h-100">
                <div class="card-body text-center">
                     <div class="fs-1 text-success mb-2">👤</div>
                     <h2 class="fw-bold">{{ usersCount }}</h2>
                     <p class="text-muted mb-0">Utilisateurs inscrits</p>
                </div>
          </div>
     </div>

     <div class="col-md-4">
          <div class="card border-0 shadow-sm h-100">
                <div class="card-body text-center">
                     <div class="fs-1 text-success mb-2">🚗</div>
                     <h2 class="fw-bold">{{ trajetsCount }}</h2>
                     <p class="text-muted mb-0">Trajets totaux</p>
                </div>
          </div>
     </div>

     <div class="col-md-4">
          <a href="{{ path('admin_trajets_index', { date: 'today' }) }}"
              class="text-decoration-none text-reset">
                <div class="card shadow-sm text-center h-100 hover-lift">
                     <div class="card-body">
                          <div class="fs-2">📅</div>
                          <div class="fs-1 fw-bold text-success">
                                {{ trajetsToday }}
                          </div>
                          <div class="text-muted">
                                Trajets aujourd’hui
                          </div>
                     </div>
                </div>
          </a>
     </div>

</div>

{# ================================
   ZONE INFO ADMIN
================================ #}
<div class="card border-0 shadow-sm bg-light">
    <div class="card-body d-flex align-items-center gap-3">
        <div class="fs-2">ℹ️</div>
        <div>
            <p class="mb-1 fw-semibold">Espace administration EcoRide</p>
            <p class="mb-0 text-muted small">
                Utilise les sections ci-dessus pour gérer les utilisateurs,
                les trajets et la comptabilité des tokens.
            </p>
        </div>
    </div>
</div>

{% endblock %}
