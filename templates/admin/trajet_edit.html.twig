{% extends 'admin/_layout.html.twig' %}

{% block admin_content %}

<h2 class="text-success mb-4 fw-bold">Modifier le trajet #{{ trajet.id }}</h2>

<div class="card p-4 shadow-sm rounded-4">
  {{ form_start(form) }}
    {{ form_widget(form) }}

    <div class="d-flex justify-content-end gap-2 mt-4">
      <a href="{{ path('admin_trajets_index') }}" class="btn btn-outline-secondary rounded-pill px-4">
        Retour
      </a>
      <button class="btn btn-success rounded-pill px-4">
        Enregistrer
      </button>
    </div>
  {{ form_end(form) }}
</div>

{% endblock %}
