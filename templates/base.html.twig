<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="EcoRide — plateforme de covoiturage écologique. Changeons d’air, pas de Terre.">
    <meta name="author" content="Quentin Neveux">

    <title>{% block title %}EcoRide - Covoiturage écologique{% endblock %}</title>

    <!-- Favicon -->
    <link rel="icon" type="image/png" href="{{ asset('assets/images/ecorideLogoGris.png') }}">

    <!-- Polices & Icônes -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Thème EcoRide (variables) -->
    <style>
        :root {
            --eco-primary:   #1b3f15;
            --eco-secondary: #3f753f;
            --eco-accent:    #ffc77e;
            --eco-dark:      #0f240c;
            --eco-light:     #f5f0e6;

            --font-title: "Merriweather", serif;
            --font-body:  "Poppins", sans-serif;

            --bs-primary:   var(--eco-primary);
            --bs-secondary: var(--eco-secondary);
            --bs-success:   var(--eco-secondary);
            --bs-warning:   var(--eco-accent);
            --bs-dark:      var(--eco-dark);
            --bs-light:     var(--eco-light);

            --bs-body-bg:   var(--eco-light);
            --bs-body-color: var(--eco-dark);
        }

        body {
            font-family: var(--font-body);
            background-color: var(--bs-body-bg);
            color: var(--bs-body-color);
            overflow-x: hidden;
        }

        h1, h2, h3, h4, h5 {
            font-family: var(--font-title);
            color: var(--eco-primary);
            font-weight: 700;
        }
    </style>

    <!-- Styles globaux -->
    <link rel="stylesheet" href="{{ asset('css/main.css') }}">

    {% block stylesheets %}{% endblock %}
</head>

<body class="vertical-bg">

    {% include 'partials/header.html.twig' %}

    {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="alert alert-{{ label }} flash-message text-center">
                {{ message }}
            </div>
        {% endfor %}
    {% endfor %}

    <main class="main-content vertical-bg">
        {% block body %}{% endblock %}
    </main>

    {% include 'partials/footer.html.twig' %}

    <!-- JS Bootstrap -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <!-- JS EcoRide global -->
    <script src="{{ asset('assets/app.js') }}"></script>

    {% block javascripts %}{% endblock %}

    <!-- Bandeau cookies -->
    <div id="cookie-banner" class="cookie-banner" role="alert" aria-live="polite">
      <p>Nous utilisons des cookies sur notre site internet pour vous offrir une expérience de navigation aussi douce et moelleuse que de vrais cookies ! 🍪</p>
      <div class="d-flex gap-2">
        <button id="accept-cookies" class="btn btn-success btn-sm">Accepter</button>
        <button id="reject-cookies" class="btn btn-outline-light btn-sm">Refuser</button>
      </div>
    </div>
    <!-- Loader de page -->
    <div id="page-loader" class="page-loader d-none">
    <div class="loader-circle"></div>
    </div>

</body>
</html>
