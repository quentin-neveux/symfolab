{% extends 'emails/_layout.html.twig' %}

{% block body %}
  <h2 style="margin:0 0 12px; font-size:20px; line-height:1.3; color:#1f7a4f;">
    Nouveau signalement 🚨
  </h2>

  <p style="margin:0 0 14px;">
    Un signalement vient d’être créé et nécessite une vérification.
  </p>

  <div style="padding:14px; border:1px solid #e6e6e6; border-radius:12px; background:#fafafa; margin:16px 0;">
    <p style="margin:0 0 8px;"><strong>Signalement :</strong> #{{ dispute.id }}</p>
    <p style="margin:0 0 8px;"><strong>Trajet :</strong> #{{ trajet.id }} — {{ trajet.villeDepart }} → {{ trajet.villeArrivee }}</p>
    <p style="margin:0 0 8px;"><strong>Reporter :</strong> {{ reporter.email ?? '—' }}</p>
    <p style="margin:0 0 8px;"><strong>Cible :</strong> {{ target.email ?? '—' }}</p>

    <p style="margin:0 0 8px;">
      <strong>Raison :</strong>
      <span style="display:inline-block; padding:3px 10px; border-radius:999px; background:#6c757d; color:#fff; font-size:12px;">
        {{ dispute.reason }}
      </span>
    </p>

    {% if dispute.message %}
      <p style="margin:0;">
        <strong>Détails :</strong><br>
        {{ dispute.message|nl2br }}
      </p>
    {% else %}
      <p style="margin:0; color:#666;">Aucun détail supplémentaire.</p>
    {% endif %}
  </div>

  <p style="margin:0;">
    <a href="{{ adminUrl }}"
       style="display:inline-block; padding:10px 16px; border-radius:999px; text-decoration:none; background:#1f7a4f; color:#fff; font-weight:bold;">
      Ouvrir la fiche admin
    </a>
  </p>
{% endblock %}
